# Permanent BORIS compatibility destination

The working portable identity remains at `agents/BORIS-001/`; the working runtime remains at `boris/`. The permanent `boris` role validates the invocation contract and returns an explicit route-only dispatch to those paths through `boris/src/identity/permanent-workforce.ts`. Routing never claims BORIS executed the work. Identity, personality, tests and runtime aliases remain unchanged.
