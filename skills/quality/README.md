# Quality skill pack

Canonical index for permanent Quality Gate contracts, trusted evidence admission, runtime, existing suites, CI and GStack evidence sources. This folder is not an executable standalone skill; `PACK.json` maps the reusable surfaces without duplicating them. Indexed evidence sources do not become trusted merely by appearing here: an authorized adapter must verify provenance before Quality Gate may count them.
